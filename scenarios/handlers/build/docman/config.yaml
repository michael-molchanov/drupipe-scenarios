---
defaultActionParams:
  PipelineController:
    buildHandler:
      handler: Docman

action:
  build:
    handler:
      actions:
        action: action.build.docman
        params:
          param1: param1
