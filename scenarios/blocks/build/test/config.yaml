---
blocks:
  build-test:
    nodeName: default
    withDocker: true
    dockerImage: michaeltigr/zebra-build-php-drush-docman
    stages:
      - name: build
        actions:
          - |
            echo "test"
            ls -l
