---
blocks:
  build:
    nodeName: default
    nodeName1: default1
    withDocker: true
    dockerImage: michaeltigr/zebra-build-php-drush-docman
    test:
      test2:
        - build
        - build1
    test3:
      - build
      - build1
        # actions:
          # - action: build
    stages:
      - test
      # - name: build
        # actions:
          # - action: build
