---
blocks:
  build:
    nodeName: default
    nodeName1: default1
    withDocker: true
    dockerImage: michaeltigr/zebra-build-php-drush-docman
    test:
      - name: build
        name1: build
        # actions:
          # - action: build
    # stages:
      # name: build
        # actions:
          # - action: build
