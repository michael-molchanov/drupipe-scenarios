---
blocks:
  build:
    nodeName: default
    nodeName1: default1
    withDocker: true
    dockerImage: michaeltigr/zebra-build-php-drush-docman
    test:
      - build
      - build1
        # actions:
          # - action: build
    # stages:
      # name: build
        # actions:
          # - action: build
