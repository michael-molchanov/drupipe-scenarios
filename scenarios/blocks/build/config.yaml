---
blocks:
  build:
    nodeName: default
    nodeName1: default1
    withDocker: true
    dockerImage: michaeltigr/zebra-build-php-drush-docman
    # test: test
    # stages:
      # name: build
        # actions:
          # - action: build
