from:
  - v2/options/deploy/commands/drupal/composer

params:
  actions:
    Commands:
      deploy:
        dev:
          params:
            actions:
              - 'echo "Add shared files."'
              - 'ln -sfn ${context.environments[context.environment].root}/shared/docroot/sites/default/files ${context.environments[context.environment].root}/current/docroot/sites/default/files'
              - 'echo "Set environment."'
              - 'ln -sfn ${context.environments[context.environment].root}/shared/environments/env.settings.php ${context.environments[context.environment].root}/current/environments/env.settings.php'
