---
params:
  containers:
    common:
      build:
        params:
          name: build
          blocks:
            - actions:
              - from: .params.actions.YamlFileHandler.build
                name: YamlFileHandler

tags:
  - yaml
