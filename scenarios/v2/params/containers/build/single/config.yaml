---
from:
  - roles/single/environments/dev
  - roles/single/environments/stage
  - roles/single/environments/prod

params:
  containers:
    common:
      build:
        params:
          name: build
          blocks:
          - actions:
            - from: .params.actions.YamlFileHandler.build
              name: YamlFileHandler

tags:
  - yaml
