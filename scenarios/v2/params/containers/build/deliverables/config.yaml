---
params:
  containers:
    common:
      build:
        params:
          name: build
          blocks:
            - actions:
              - from: .params.actions.Docman.build
                name: Docman

tags:
  - docman
