---
params:
  containers:
    common:
      artifact:
        params:
          name: artifact
          blocks:
          - actions:
            - from: .params.actions.PipelineController.retrieveArtifact
              name: PipelineController
              methodName: retrieveArtifact
              artifactHandler:
                handler: GitArtifact
                method: retrieve

tags:
  - gitartifact
